# RKNN YOLO 迁移计划（Sc_vision/uav）

目标：在 /home/setsuna/RM/AutoAim/.wt_Sc_vision_uav 上，基于现有 Sc_vision/uav 工作树，吸收 sp_vision_25 的 yolov5_rknn 与 uav_rknn_mt 能力，但不回退 Sc_vision 已有的功能。

原则：
1. 以 uav 工作树当前状态为主，不覆盖已有未提交工作。
2. 以“最小侵入”方式迁移，只补缺口，不把 Sc_vision 已有增强删掉。
3. 先修正错误迁移，再补新能力，再接入口，再验证。

已确认事实：
- uav 工作树已经有部分 RKNN 迁移，但同时错误地删掉了 Sc_vision 原有能力：
  - get_debug_roi 接口
  - yolov5 的 infer_request_pool
  - dynamic ROI
  - 顶层 CMake 的 OpenVINO / fastcdr 配置
  - rp24 分支兼容
- src/uav.cpp 仍是传统 Detector 路径，还没有真正接到 RKNN YOLO。
- uav 工作树里还没有 uav_rknn_mt.cpp 入口文件。

执行步骤：
1. 恢复并保留 Sc_vision 原有能力
   - yolo.hpp / yolo.cpp：保留 get_debug_roi，同时增加 submit/wait/try_wait
   - yolov5.hpp / yolov5.cpp：保留 infer_request_pool、dynamic ROI、rp24 支持
   - yolo11 / yolov8：保留 get_debug_roi
   - 顶层 CMake：保留 OpenVINO / fastcdr，增量加入 RKNN 检测逻辑

2. 补齐 RKNN YOLO 能力
   - 保留并检查 yolov5_rknn.hpp / yolov5_rknn.cpp
   - 保留 rknn_threadpool 目录
   - tasks/auto_aim/CMakeLists.txt 做“条件编译 RKNN”而不是强制替换原路径

3. 接入 UAV 入口
   - 新增 src/uav_rknn_mt.cpp，采用 sp_vision_25 的异步 submit/wait 流程
   - 尽量保留 Sc_vision 的 auto_buff 与已有代码风格
   - CMake 新增可执行文件 uav_rknn_mt
   - 视情况决定是否修改 src/uav.cpp；优先新增独立入口，避免破坏原有 uav

4. 配置与验证
   - 在 configs/uav.yaml 增加 RKNN 所需配置：
     - yolov5_rknn_model_path
     - yolov5_rknn_thread_num
     - use_traditional
   - 做 cmake / build 级验证
   - 如果宿主缺 RKNN / RGA 库，明确报告阻塞点

注意：
- 不动 assets 下已有未跟踪 rknn 模型文件的内容，只在构建与配置层面引用。
- 优先保持 Sc_vision 的 yolov5 / yolov8 / yolo11 / rp24 可继续工作。
